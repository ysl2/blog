# cc-switch-cli

## Installation

```bash
cd ~/Documents
git clone git@github.com:SaladDay/cc-switch-cli.git
cd cc-switch-cli/src-tauri
cargo build --release
cp target/release/cc-switch ~/.vocal/cc-switch
```
