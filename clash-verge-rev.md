# clash-verge-rev

## Configuration

- For Linux, enable TUN service

  ```bash
  sudo clash-verge-service-install
  sudo systemctl daemon-reload
  sudo systemctl enable --now clash-verge-service
  sudo systemctl status clash-verge-service
  ```
